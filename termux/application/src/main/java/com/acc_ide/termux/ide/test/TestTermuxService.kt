package com.acc_ide.termux.ide.test

import com.acc_ide.termux.app.TermuxService

class TestTermuxService {
    fun testAccess(service: TermuxService) {
        // Just test if we can access the class
        val test: TermuxService = service
    }
}